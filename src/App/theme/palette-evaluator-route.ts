import { teal } from "@mui/material/colors";
import type { PaletteOptions } from "@mui/material/styles";

export const PALETTE_EVALUATOR_ROUTE: PaletteOptions = {
  primary: { main: teal[200] },
  tonalOffset: 0.5,
  contrastThreshold: 9,
};
